country_decisions = {

	italian_nation = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = formed_italy_flag }
			NOT = { exists = ITA }
			NOT = { tag = PAP }
			NOT = { tag = HLR }
			NOT = { tag = Z17 }
			OR = { 
				culture_group = latin
				primary_culture = idali
				primary_culture = old_lombard
			}
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				AND = {
					region = italy_region
					root = {
						NOT = {
							num_of_owned_provinces_with = {
								value = 15
								region = italy_region
								is_core = root
							}
						}
						owns_core_province = 112	
						owns_core_province = 104	
						owns_core_province = 116	
						owns_core_province = 113	
					}
				}
				province_id = 112	# Venice
				province_id = 104	# Milan
				province_id = 116	# Firenze
				province_id = 113	# Ferrara
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			adm_tech = 6
			is_at_war = no
			if = {
				limit = { overlord = { tag = Z17 } }
				OR = {
					owns_core_province = 112	# Venice
					overlord = { owns_core_province = 112 }
				}
				OR = {
					owns_core_province = 104	# Milan
					overlord = { owns_core_province = 104 }
				}
				OR = {
					owns_core_province = 116	# Firenze
					overlord = { owns_core_province = 116 }
				}
				OR = {
					owns_core_province = 113	# Ferrara
					overlord = { owns_core_province = 113 }
				}
			}
			else = {
				owns_core_province = 112	# Venice
				owns_core_province = 104	# Milan
				owns_core_province = 116	# Firenze
				owns_core_province = 113	# Ferrara
			}
			num_of_owned_provinces_with = {
				value = 15
				region = italy_region
				is_core = root
			}
		}
		effect = {
			hidden_effect = { ia_save_historical_relations_effect = yes }
			change_tag = ITA
			on_change_tag_effect = yes
			#this is so that a tier 1 reform is always selected, preventing the user from just never selecting one and enjoying loads of absolutism
			if = {
				limit = {
					NOT = { has_reform_on_level = 0 }
				}
				add_government_reform = signoria_reform
			}
			italy_region = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = ITA
			}
			add_prestige = 25
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			swap_non_generic_missions = yes
			set_country_flag = formed_italy_flag
		}
		ai_will_do = {
			factor = 400
		}
	}

	gods_kingdom = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			#OR = {
			#	is_free_or_tributary_trigger = yes
			#	ai = no
			#}
			tag = PAP
			NOT = { has_country_flag = is_kingdom_of_god_flag }
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_nomad = no
			is_at_war = no
			reform_level = 3
			OR = {
				custom_trigger_tooltip = {
					tooltip = ia_pap_antipapacy_ended_tt
					has_global_flag = catholic_antipopes_ended
				}
				total_own_and_non_tributary_subject_development = 1000
			}
		}
		effect = {
			#country_event  = { id = catholic_flavor.2 }
			add_government_reform = kingdom_of_god_reform
			override_country_name = KINGDOM_OF_GOD
			set_country_flag = is_kingdom_of_god_flag
		}
		ai_will_do = {
			factor = 1
		}
	}

}
