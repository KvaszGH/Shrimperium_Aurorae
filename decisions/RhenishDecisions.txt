country_decisions = {
	rhe_prospect_for_coal_decision = {
		major = yes
		color = { 255 126 0 }
		potential = {
			has_country_flag = rhe_enable_early_coal_decision
			NOT = { is_year = 1700 }
		}
		provinces_to_highlight = {
			has_latent_trade_goods = coal
			owned_by = ROOT
		}
		allow = {
			custom_trigger_tooltip = {
				tooltip = prospect_for_coal_rhe_tt
				OR = {
					had_country_flag = {
						flag = rhe_coal_deposit_cooldown
						days = 1095
					}
					NOT = { has_country_flag = rhe_coal_deposit_cooldown }
				}
			}
			government_reform_progress = 10
			adm = 100
			dip = 50
			treasury = 800
		}
		effect = {
			clr_country_flag = rhe_enable_early_coal_decision
			set_country_flag = rhe_enable_early_coal_decision
			change_government_reform_progress = -10
			add_dip_power = -100
			add_dip_power = -50
			add_treasury = -800
			hidden_effect = { 
				export_to_variable = {
        			variable_name = RheinlandCoalSpawningCooldown
        			value = trigger_value:is_year
        		}
    		}
    		random_owned_province = {
    			limit = { has_latent_trade_goods = coal }
    			change_trade_goods = coal
    			remove_latent_trade_goods = coal
    			if = {
    				limit = { 
    					NOT = { has_country_flag = rhe_coal_regained_value }
    				}
    				add_permanent_province_modifier = {
						name = rhe_coal_unknown_uses
						duration = -1
					}
				}
    		} 
		}
		ai_will_do = {
			factor = 1000
		}
	}
}